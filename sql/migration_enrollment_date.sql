-- ============================================================
-- 마이그레이션: 학생 입학일 컬럼 추가 (재학중 취득 자동판정용)
-- 이미 schema.sql을 실행해 운영 중인 DB에서 딱 1번만 실행하세요.
-- ============================================================
ALTER TABLE users
  ADD COLUMN enrollment_date DATE NULL COMMENT '입학일 (재학중 취득 자동판정 기준, 필수 2종은 예외)'
  AFTER status;
